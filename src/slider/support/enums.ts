enum Position {
  Min = 'min',
  Max = 'max',
}

enum SliderEvent {
  SliderClick,
  PointMove,
  StopPointMove,
  ValueChanged,
}

enum ClassNames {
  Point,
  Scale,
  Tooltip,
  Range,
  Body,
  Line,
}

export { Position, SliderEvent, ClassNames };
